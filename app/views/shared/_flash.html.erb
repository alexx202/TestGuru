<div class="container mt-3">
  <div class="row justify-content-center mt-3">
    <div class="col col-md-4">
      <% flash.each do |key, message| %>
        <%= content_tag :p, message, class: "alert alert-#{key}", role: 'alert' %>
      <% end %>
    </div>
  </div>
</div>

<ul class="nav ul">
  <% if user_signed_in? %>
      <li><%= link_to t('.testguru'), root_path %></li>
      <li><%= link_to t('.logout'), destroy_user_session_path, method: :delete %></li>
  <% else %>
      <li><%= link_to t('.sign_up'), new_user_registration_path %></li>
      <li><%= link_to t('.sign_in'), new_user_session_path %></li>
  <% end %>
</ul>
